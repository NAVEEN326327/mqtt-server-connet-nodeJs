{
  "name": "mqtt_server",
  "version": "1.0.0",
  "description": "mqtt server",
  "main": "communicate_to_mqtt.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Naveen",
  "license": "ISC",
  "dependencies": {
    "mqtt": "^3.0.0"
  }
}
